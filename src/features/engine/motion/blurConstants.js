/**
 * Shared blur limits and quality for layer blur (PIXI BlurFilter).
 * - BLUR_MAX: max strength (0–BLUR_MAX). Lower = less GPU cost on low-end devices.
 * - BLUR_QUALITY: BlurFilter.quality (1 = fastest/blocky, 2 = good balance, 3+ = smoother/heavier).
 */
export const BLUR_MAX = 10
export const BLUR_QUALITY = 2
